// Given an int variable k that has already been declared, use a while loop to print a single line consisting of 88 asterisks. 
// Use no variables other than k.


k = 0;
while (k < 88){
   System.out.print("*");
   k = k + 1;
}

// if you want to use it with javascpript.

function anything(){

    total = 0;
    for (k=0; k <= 50; k++){
       globe = total + (k*k);
      console.log(globe)
      }
    
    }
    anything()